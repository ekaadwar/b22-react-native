import React, { Component } from "react";
import { View, ScrollView, StyleSheet, Text } from "react-native";

export default class PromoProducts extends Component {
  render() {
    return (
      <ScrollView>
        <Text>Ini Halaman Promo Products</Text>
      </ScrollView>
    );
  }
}
